# crypt

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build crypt` to build the library.

## Running unit tests

Run `nx test crypt` to execute the unit tests via [Jest](https://jestjs.io).
